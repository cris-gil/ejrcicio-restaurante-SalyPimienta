// Falta fuente de datos
const controller = {
    index: (req, res) => {
        res.render('index')
    },

    menu: (req, res) => {
        res.render('detalleMenu')
    }
}

// Falta nustra fuente de datos

// exportar el resultado
module.exports = controller;